_list = [1,2,3,4,5]
    # Here we have defined a list but what we have actually done is created an instance(object) of list class
        # list and many other class are provided by python by default
        # For example
_list.append(6)
        # Here we have used append method which is part of list class
print(_list)