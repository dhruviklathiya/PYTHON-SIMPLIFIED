# pass keyword in python
# Keyword pass will be used mostly inside if-else block when we want to leave block empty
# If we leave an empty if block then python will throw error for resolving this error we will use pass keyword
# Indentation and if-else block in python

abc = 10
if abc==10:
    pass
else:
    print(f"Value of abc is not 10")

print("IF_ELSE executed successfully")

# Following code will generate error:
# abc = 10
# if abc==10:

# else:
    # print(f"Value of abc is not 10")

print("IF_ELSE executed successfully")