# Tuple unpacking
    # Same as destructuring in javascript but we must have to unpack all elements
_tuple = (1,2,3,4,5)
_one,_two,_three,_four,_five = _tuple
print(_one)
print(_two)
print(_three)
print(_four)
print(_five)

# We cannot change tuple but list inside tuple are muttable